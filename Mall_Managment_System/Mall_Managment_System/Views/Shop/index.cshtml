@model IEnumerable<Mall_Managment_System.Models.Shops>

@{
    ViewData["Title"] = "Index";
}
<div class="container  pt-4 px-4  align-content-center">
    <div class="row g-4">

        <div class="col-sm-12 col-xl-6 w-100">
            <div class="bgcolor rounded h-100 p-4">
                <div class="row">
                    <div class="col-md-6">
                        <h4 class="mb-4">All Shops</h4>

                    </div>
                    <div class="col-md-6 text-sm-end"><button class=" btn btn-outline-danger"> <a asp-controller="Shop" asp-action="Addproduct">Add-New</a></button></div>
                </div>
                <table class="table table-hover">
                    <thead class="text-white">
        <tr>
                            <th scope="col">#</th>
                            <th scope="col">Shop Name</th>
                            <th scope="col">Description</th>
                            <th scope="col">Image</th>
                            <th scope="col">Actions </th>
        </tr>
    </thead>
                    <tbody class="text-white-50">
                @foreach (var item in Model) {
                        <tr>
                                <td scope="row">@Html.DisplayFor(modelItem => item.ID)</td>
                                <td scope="row">@Html.DisplayFor(modelItem => item.Name)</td>
                                <td scope="row">@Html.DisplayFor(modelItem => item.Description)</td>
                                <td scope="row"><img src="~/images/@Html.DisplayFor(modelItem => item.Image)" style="width:60px;height:60px;" /></td>
                                <td scope="row">
                                    <a class="btn btn-outline-info me-2" asp-controller="Shop" asp-action="Edit" asp-route-id="@item.ID">Edit</a>
                                    <a class="btn btn-outline-primary" asp-controller="Shop" asp-action="Delete" asp-route-id="@item.ID">Delete</a>
                

            </td>
        </tr>
}
    </tbody>
</table>
            </div>
        </div>
    </div>
</div>

<!-- Modal -->
@* <div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Modal title</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <form method="post" asp-action="" enctype="multipart/form-data">
            <div class="modal-body">
               
                <input type="text" name="shopid" readonly id="shopid" />
                <br />
                <input type="text" name="shopname" readonly id="shopname" />
                <br />
                <input type="text" name="shopdesc" readonly id="shopdesc" />
                <br />
                <input type="file" name="shopimage" readonly id="shopimage" />

               
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                <button type="submit" class="btn btn-primary">Save changes</button>
            </div>
            </form>
        </div>
    </div>
</div> *@

@* <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>

                                <script>
                                   $(document).ready(function(){

                                        $(document).on("click", "#editbtn", function(){
                                            //alert("");

                                            var id = $(this).attr("data-id");
                                            //console.log(id);
                                            $.ajax({
                                                url: "shop/getdata/" + id,
                                                type: "POST",
                                                success:function(response) {

                                                    $("#exampleModal").modal("show");
                    $("#shopid").val(response.ID);
                                                    $("#shopname").val(response.ID);
                    $("#shopdesc").val(response.Shop_Description);
                    $("#shopimage").val(response.Shop_Image);
                                                }
                                            });
                                        });
                                    });

                                </script> *@